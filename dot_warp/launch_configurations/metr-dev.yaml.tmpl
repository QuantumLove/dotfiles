{{- if .is_macos }}
name: METR Development
windows:
  - tabs:
    - title: "inspect-action"
      layout:
        cwd: {{ .chezmoi.homeDir }}/code/hawk
        commands:
          - exec: source ~/.sh_functions && awstg && echo "🦅 Hawk (inspect-action) environment ready"
    - title: "mp4-deploy"
      layout:
        cwd: {{ .chezmoi.homeDir }}/code/mp4-deploy
        commands:
          - exec: source ~/.sh_functions && awstg && kubectl config use-context metr-dev && echo "🚀 MP4 Deploy ready"
    - title: "IAM & Auth"
      layout:
        cwd: {{ .chezmoi.homeDir }}/code/metr-iam
        commands:
          - exec: source ~/.sh_functions && awsprod && echo "🔐 IAM Management - Production!"
    - title: "Claude Code"
      layout:
        cwd: {{ .chezmoi.homeDir }}/.local/share/chezmoi
        commands:
          - exec: echo "🤖 Claude Code ready"
{{- end }}